<Window x:Class="ReservasApp.Views.ReservasObjetosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ReservasApp.ViewModels"
        Title="Reservas (Objetos) — Conectado"
        Height="500" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="#F2F4F7">

    <Window.DataContext>
        <vm:ReservasObjetosViewModel />
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="BotonBuscar" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Background" Value="#2F6FED" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Reservas (Objetos)" FontSize="18" FontWeight="Bold" Foreground="#1D2939" />
            <TextBlock Text="Modo conectado — SqlDataReader" FontSize="12" Foreground="#667085" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Buscar por fecha:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#344054" />
            <DatePicker SelectedDate="{Binding FechaBusqueda}" Width="160" Margin="0,0,8,0" />
            <Button Content="Buscar" Style="{StaticResource BotonBuscar}" Command="{Binding BuscarCommand}" />
        </StackPanel>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.10" />
            </Border.Effect>

            <DataGrid ItemsSource="{Binding Reservas}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      RowHeight="34"
                      BorderThickness="0"
                      Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding ReservaId}" Width="45" />
                    <DataGridTextColumn Header="Aula" Binding="{Binding NombreAula}" Width="*" />
                    <DataGridTextColumn Header="Usuario" Binding="{Binding NombreUsuario}" Width="*" />
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="90" />
                    <DataGridTextColumn Header="Hora" Binding="{Binding Hora}" Width="70" />
                    <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>