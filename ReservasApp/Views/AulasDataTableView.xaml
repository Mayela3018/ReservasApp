<Window x:Class="ReservasApp.Views.AulasDataTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ReservasApp.ViewModels"
        Title="Aulas (DataTable) — Desconectado"
        Height="480" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F2F4F7">

    <Window.DataContext>
        <vm:AulasDataTableViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Aulas (DataTable)" FontSize="18" FontWeight="Bold" Foreground="#1D2939" />
            <TextBlock Text="Modo desconectado — SqlDataAdapter.Fill" FontSize="12" Foreground="#667085" />
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.10" />
            </Border.Effect>

            <DataGrid ItemsSource="{Binding Aulas}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      RowHeight="34"
                      BorderThickness="0"
                      Background="White" />
        </Border>
    </Grid>
</Window>
    